package com.unicen.core.configuration;

public enum AppEnvironment {
    DEV, // for local development
    TEST, // for local testing
    INT, // for running integration tests
    QA, // for running QA app
    PROD, // for running PROD app
    PREPROD
}
